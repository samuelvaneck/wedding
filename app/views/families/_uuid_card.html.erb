<%= react_component 'card-uuid', { title: 'Bruiloft code', spinner: false } %>

<script>
  // load script
  const uuidParams = getUrlParameter("uuid");
  var rsvp = new window.RSVP();
  !!uuidParams ? rsvp.code(uuidParams) : rsvp.code();
  if (rsvp.code()) {
    rsvp.fillInCode();
  }
</script>
