<!DOCTYPE html>
<html>
  <head>
    <title>Wedding</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
        <% if params[:controller].split("/").first == "admin" %>
          <div class="col-1 col-md-2" style="padding:0px">
            <%= render 'layouts/sidenav' %>
          </div>
        <% end %>
        <div class="col">
          <% if user_signed_in? %>
            <div class="admin-btns">
              <%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'btn btn-outline-primary float-right mt-2 ml-2' %>
              <% if params[:controller].split("/").first != "admin" %>
                <%= link_to "Admin", admin_families_path, class: "btn btn-outline-primary float-right mt-2" %>
              <% end %>
            </div>
          <% end %>
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>

<script>
  bindDataUploadBtn($('#visible_photo_field'));
</script>
